{
  "build": {
    "builder": "dockerfile",
    "context": ".",
    "dockerfile": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
